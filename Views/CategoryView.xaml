<UserControl x:Class="Quizly.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF0F172A">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="←"
                    Width="40" Height="40"
                    Background="#1F2937" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="Back_Click"/>

            <TextBlock Grid.Column="1"
                       Text="Kategorie wählen"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="2" Rows="3" Margin="0,0,0,14">
            <Button Content="Geschichte" Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
            <Button Content="Sport"      Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
            <Button Content="Gaming"     Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
            <Button Content="Wissen"     Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
            <Button Content="Filme"      Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
            <Button Content="Musik"      Margin="6" Height="100" Background="#111B33" Foreground="White" BorderThickness="0"/>
        </UniformGrid>

        <Button Grid.Row="2"
                Content="Starten"
                Height="56"
                Background="#60A5FA"
                Foreground="White"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Click="Start_Click"/>
    </Grid>
</UserControl>
