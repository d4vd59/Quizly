<UserControl x:Class="Quizly.Views.LeaderboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF0F172A">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="←"
                    Width="40" Height="40"
                    Background="#1F2937" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="Back_Click"/>

            <TextBlock Grid.Column="1"
                       Text="Rangliste"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="#111B33" CornerRadius="18" Padding="12">
            <ListView Background="Transparent" BorderThickness="0">
                <ListViewItem Content="1. PlayerOne  •  12.500" Foreground="White"/>
                <ListViewItem Content="2. QuizMaster •  11.800" Foreground="White"/>
                <ListViewItem Content="3. Lara       •  10.200" Foreground="White"/>
                <ListViewItem Content="4. Max        •   9.900" Foreground="White"/>
                <ListViewItem Content="5. You        •   9.100" Foreground="#93C5FD"/>
            </ListView>
        </Border>
    </Grid>
</UserControl>
