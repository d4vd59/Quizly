<UserControl x:Class="Quizly.Views.QuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF0F172A">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="←"
                    Width="40" Height="40"
                    Background="#1F2937" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="Back_Click"/>

            <TextBlock Grid.Column="1"
                       Text="Frage"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="#111B33" CornerRadius="18" Padding="16">
            <StackPanel Margin="12">
                <TextBlock Text="Welche Stadt war früher Konstantinopel?"
                           Foreground="White" FontSize="20" FontWeight="Bold" TextWrapping="Wrap"/>

                <Button Content="A) Istanbul" Height="56" Background="#1F2937" Foreground="White" BorderThickness="0" Click="Answer_Click"/>
                <Button Content="B) Athen"    Height="56" Background="#1F2937" Foreground="White" BorderThickness="0" Click="Answer_Click"/>
                <Button Content="C) Rom"      Height="56" Background="#1F2937" Foreground="White" BorderThickness="0" Click="Answer_Click"/>
                <Button Content="D) Kairo"    Height="56" Background="#1F2937" Foreground="White" BorderThickness="0" Click="Answer_Click"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="(Demo) Klick auf Antwort → Ergebnis"
                   Foreground="#64748B" HorizontalAlignment="Center" Margin="0,14,0,0"/>
    </Grid>
</UserControl>
